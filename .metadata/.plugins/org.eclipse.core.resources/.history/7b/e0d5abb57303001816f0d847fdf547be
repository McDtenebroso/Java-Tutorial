package com.keenum.calcengine;

public class Multiple extends CalculateBase {

	public Multiple(){}
	
	public Multiple(double leftVal, double rightVal)
	{
		super(leftVal, rightVal);
	}
	
	@Override
	public void calculate() 
	{
		double value = getLeftVal() * getRightVal();
		setResult(value);
	}
	
}
